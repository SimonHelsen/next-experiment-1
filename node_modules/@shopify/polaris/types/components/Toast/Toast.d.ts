import * as React from 'react';
import { FrameContext } from '../Frame';
import { WithAppProviderProps } from '../AppProvider';
export interface ToastProps {
    /** The content that should appear in the toast message */
    content: string;
    /**
     * The length of time in milliseconds the toast message should persist
     * @default 5000
     */
    duration?: number;
    /** Display an error toast. */
    error?: boolean;
    /** Callback when the dismiss icon is clicked */
    onDismiss(): void;
}
export declare type Props = ToastProps & WithAppProviderProps;
export declare class Toast extends React.PureComponent<Props, never> {
    static contextTypes: {
        frame: any;
    };
    context: FrameContext;
    private id;
    private appBridgeToast;
    componentDidMount(): void;
    componentWillUnmount(): void;
    render(): null;
}
declare const _default: React.ComponentClass<ToastProps, React.ComponentState> & typeof Toast;
export default _default;
